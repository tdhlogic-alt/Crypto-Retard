# Ignore Gradle caches and generic build artifacts
.gradle/
.gradle
**/target/
*.jar

# Keep the prebuilt Spring Boot jar in the Docker build context.
# The GitHub Action creates this file before docker build.
!build/
!build/libs/
!build/libs/app.jar

# IDE files
.idea/
*.iml

# Git
.git

# OS
.DS_Store
Thumbs.db

# Dockerfile and docker-compose local overrides
Dockerfile
docker-compose*.yml
